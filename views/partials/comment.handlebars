<!-- views/partials/comment.handlebars -->

<div class="card" id="{{this._id}}">
  <div class="card-block">
    <h4 class="card-author">{{this.author}}</h4>
    <p class="card-text">{{this.content}}</p>
    <!-- Delete link -->
    <!-- <p><button class="btn btn-link delete-comment"  data-comment-id="{{this._id}}">Delete</button></p> -->

    </form></p>
  </div>
</div>
